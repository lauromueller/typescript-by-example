import React from 'react';
import './App.css';
import { Button } from '@material-ui/core';

const App = () => {
  return (
    <div className='App'>
      <Button color='primary' variant='contained'>
        Some text
      </Button>
    </div>
  );
};

export default App;
